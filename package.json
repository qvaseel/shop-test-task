{
	"name": "test",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"init": "npm i && cd next && npm install && cd ../server && npm install",
		"next": "concurrently \"cd server && npm run start\" \"cd next && npm run dev\""
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"concurrently": "^8.2.2"
	}
}
